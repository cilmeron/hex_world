using UnityEngine;
// using UnityEngine.AI;
/*
public class ProceduralNavMeshBaker : MonoBehaviour
{
    public MeshFilter proceduralMeshFilter; // Reference to the procedural mesh's MeshFilter component
    public NavMeshSurface navMeshSurface; // Reference to the NavMeshSurface component

    void Start()
    {
        BakeNavMesh();
    }

    void BakeNavMesh()
    {
        navMeshSurface.BuildNavMesh(); // Bake the NavMesh based on the procedural mesh
    }

    // Call this method whenever your procedural mesh changes
    void UpdateNavMesh()
    {
        navMeshSurface.RemoveData(); // Clear the existing NavMesh data
        navMeshSurface.AddData(); // Add the new procedural mesh data
        BakeNavMesh(); // Re-bake the NavMesh
    }
}
*/